{
  "name": "portfolio-root",
  "private": true,
  "scripts": {
    "install": "npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "vercel-deploy": "npm run vercel-deploy --prefix frontend"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.108.2"
  },
  "engines": {
    "node": "20.x"
  }
}
